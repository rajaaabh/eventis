events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    sendfile        on;
    keepalive_timeout 65;

    # Compression gzip pour les assets statiques
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
    gzip_min_length 1000;

    server {
        listen 80;
        server_name localhost;

        # Dossier où Nginx sert les fichiers buildés par Vite
        root /usr/share/nginx/html;
        index index.html;

        # Logs
        access_log /var/log/nginx/access.log;
        error_log  /var/log/nginx/error.log;

        # React Router — toutes les routes inconnues renvoient vers index.html
        location / {
            try_files $uri $uri/ /index.html;
        }

        # Cache des assets statiques (JS, CSS, images)
        location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
            expires 1y;
            add_header Cache-Control "public, immutable";
        }

        # Interdire l'accès aux fichiers cachés
        location ~ /\. {
            deny all;
        }
    }
}